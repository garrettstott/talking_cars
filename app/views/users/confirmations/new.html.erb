<div class='six columns offset-by-three'>
  <h2>Resend confirmation instructions</h2>

  <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
    <% devise_error_messages! %>
    <div class="field">
      <%= f.email_field :email, autofocus: true, value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), placeholder: 'Email', required: true %>
    </div>

    <div class="actions">
      <%= f.button "Resend confirmation instructions", class: 'btn', data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Sending"} %>
    </div>
  <% end %>

  <%= render "users/shared/links" %>
</div>
